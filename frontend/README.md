# Pallet Mix Scenario Modeller Frontend

React + Vite UI for the Pallet Mix Scenario Modeller.

## Setup
```bash
npm install
npm run dev
```

Default API base URL: `http://localhost:8000`.
Override with `VITE_API_BASE` if needed.
